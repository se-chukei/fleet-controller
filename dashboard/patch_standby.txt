                setConfirmAction({
                  type: 'STANDBY',
                  callback: () => {
                    if (onTriggerAction) onTriggerAction([node.id], 'STANDBY');
                  }
                });
              }}
              disabled={!controlsUnlocked || !!node.accessKeyRevoked || standbyState !== 'idle'}
